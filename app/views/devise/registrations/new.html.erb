<section id="connexion" class="text-light">
  <div class="container">
    <div class="row">
      <div class="col-md-6 offset-md-3">
        <h1>Inscription</h1>
        <hr>

        <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="field mb-3">
          <%= f.label :company, "Veuillez entrer votre nom d'entreprise :" %><br />
          <%= f.text_field :company, autofocus: true, autocomplete: "company", class: "form-control" %>
        </div>

        <div class="field mb-3">
          <%= f.label :email, "Votre email :" %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
        </div>

        <div class="field mb-3">
          <%= f.label :password, "Votre mot de passe :" %><br />
          <%= f.password_field :password, autocomplete: "new-password", class: "form-control", placeholder: "6 caractères minimum" %>
        </div>

        <div class="field mb-3">
          <%= f.label :password_confirmation, "Confirmer votre mot de passe" %><br />
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control" %>
        </div>

        <div class="actions mb-3">
          <%= f.submit "Je m'inscris", class: "form-control btn devise-btn" %>
        </div>
        <% end %>

        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</section>

